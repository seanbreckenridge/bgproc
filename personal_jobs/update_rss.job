#!/bin/sh
# update rss feeds on my computer
# sean.fish/d/update-rss
evry 15 minutes -updaterss && {
	if update-rss; then
		printlog "updaterss:updated RSS feeds:$(cat ~/.cache/rss-unread)"
	else
		printlog "updaterss:failed to update RSS feeds"
	fi
}
